# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
.pnp
.pnp.js

# Next.js
.next/
out/
build
dist

# Sanity generated runtime
.sanity/

# Production
*.local

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets: ignore every .env* by default; the placeholder template is the sole exception.
.env
.env.*
!.env.example

# Stray credential/key files must never be committed (public repo).
*.pem
*.key
*.p12
*-service-account*.json
secrets.*

# Historical backfill source exports contain school, student, and guardian PII.
data/backfill/competition-history/*.csv
data/backfill/competition-history/*.tsv
data/backfill/competition-history/*.xlsx


# Supabase CLI local state — holds the project ref and a pooler URL WITH the DB password.
supabase/.temp/
supabase/.branches/

# Internal ops docs/scripts — kept local, not in the public repo (they map the
# prod/dev/staging project refs, orgs, and backup buckets).
docs/environments-and-deploy.md
scripts/db-push-prod.sh
data/**
data/
